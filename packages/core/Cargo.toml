[package]
name = "portalenv-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core functionalities for portalenv."

[dependencies]
async-trait = "0.1"
dotenvy = "0.15.7"
regex = "1.13.1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[features]
default = []
multi-thread = []
