name: "Run test for all packages"
description: ""
runs:
  using: "composite"
  steps:
    - name: Format and lint check
      shell: bash
      run: make check

    - name: Run all tests
      shell: bash
      run: make test-all
